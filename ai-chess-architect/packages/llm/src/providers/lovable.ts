export async function providerLovable(prompt: string): Promise<string> {
  return JSON.stringify({
    headline: 'Occasion manquée',
    why_bad_or_good: 'Une tactique gagnante était disponible sur la colonne ouverte.',
    what_to_learn: ['Repérer fourchettes et clouages'],
    best_line_explained: 'La variante montre un gain de matériel forçant.'
  });
}
